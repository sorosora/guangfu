# 花蓮光復清淤地圖 - 環境變數範本
# Environment Variables Template for Hualien Guangfu Cleanup Progress Map

# ⚠️ 重要安全提醒 | Important Security Notice
# 1. 請勿將真實的 API 金鑰提交到 Git 倉庫 | Never commit real API keys to Git repository
# 2. 複製此檔案為 .env.local 並填入真實值 | Copy this file to .env.local and fill in real values
# 3. .env.local 已被 .gitignore 忽略 | .env.local is ignored by .gitignore

# ===========================================
# 🌐 前端公開變數 | Frontend Public Variables
# ===========================================
# 這些變數會暴露在瀏覽器中，請勿包含敏感資訊
# These variables are exposed to the browser, do not include sensitive information

# Supabase 公開配置 | Supabase Public Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# 圖磚 CDN 基礎 URL | Tiles CDN Base URL
NEXT_PUBLIC_TILES_BASE_URL=https://tiles.yourdomain.com

# 測試區域配置 | Test Area Configuration
# 🔧 用於開發測試的地理範圍設定 | Geographic area settings for development testing
# ⚠️ 注意：在 .env.local 中設定您的實際測試位置座標 | Note: Set your actual test location coordinates in .env.local
# 以下為範例座標，請勿用於實際開發 | The following are example coordinates, do not use for actual development

# 測試區域四個角落座標 | Test Area Corner Coordinates
NEXT_PUBLIC_TEST_AREA_NORTHWEST_LAT=25.100000
NEXT_PUBLIC_TEST_AREA_NORTHWEST_LON=121.450000
NEXT_PUBLIC_TEST_AREA_NORTHEAST_LAT=25.100000
NEXT_PUBLIC_TEST_AREA_NORTHEAST_LON=121.490000
NEXT_PUBLIC_TEST_AREA_SOUTHWEST_LAT=25.070000
NEXT_PUBLIC_TEST_AREA_SOUTHWEST_LON=121.450000
NEXT_PUBLIC_TEST_AREA_SOUTHEAST_LAT=25.070000
NEXT_PUBLIC_TEST_AREA_SOUTHEAST_LON=121.490000

# 測試區域中心點 | Test Area Center Point
NEXT_PUBLIC_TEST_AREA_CENTER_LAT=25.085000
NEXT_PUBLIC_TEST_AREA_CENTER_LON=121.470000

# ===========================================
# 🔒 後端私密變數 | Backend Private Variables
# ===========================================
# 這些變數僅在伺服器端使用，絕不會暴露到瀏覽器
# These variables are only used on the server-side, never exposed to the browser

# Supabase 服務角色金鑰 | Supabase Service Role Key
# 📍 部署位置: Vercel 環境變數 | Deployment: Vercel Environment Variables
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Upstash Redis 配置 | Upstash Redis Configuration
# 📍 部署位置: Vercel + GitHub Secrets | Deployment: Vercel + GitHub Secrets
UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_redis_token

# Cloudflare R2 配置 | Cloudflare R2 Configuration
# 📍 部署位置: GitHub Secrets (僅用於 GitHub Actions) | Deployment: GitHub Secrets (GitHub Actions only)
CLOUDFLARE_R2_ACCESS_KEY_ID=your_access_key_id
CLOUDFLARE_R2_SECRET_ACCESS_KEY=your_secret_access_key
CLOUDFLARE_R2_BUCKET_NAME=your_bucket_name

# ===========================================
# 📋 部署指引 | Deployment Guide
# ===========================================

# 🖥️  本地開發 | Local Development
# 1. 複製此檔案: cp .env.example .env.local
# 2. 填入所有變數的真實值
# 3. 啟動開發伺服器: yarn dev

# ☁️  Vercel 部署 | Vercel Deployment
# 需要設定的環境變數 | Required environment variables:
# - NEXT_PUBLIC_SUPABASE_URL
# - NEXT_PUBLIC_SUPABASE_ANON_KEY
# - NEXT_PUBLIC_TILES_BASE_URL
# - NEXT_PUBLIC_KMZ_BASE_URL
# - SUPABASE_SERVICE_ROLE_KEY
# - UPSTASH_REDIS_REST_URL
# - UPSTASH_REDIS_REST_TOKEN
#
# 📍 測試區域配置（可選）| Test Area Configuration (Optional):
# - NEXT_PUBLIC_TEST_AREA_NORTHWEST_LAT/LON
# - NEXT_PUBLIC_TEST_AREA_NORTHEAST_LAT/LON
# - NEXT_PUBLIC_TEST_AREA_SOUTHWEST_LAT/LON
# - NEXT_PUBLIC_TEST_AREA_SOUTHEAST_LAT/LON
# - NEXT_PUBLIC_TEST_AREA_CENTER_LAT/LON
# - NEXT_PUBLIC_TEST_AREA_DESCRIPTION

# 🔧 GitHub Actions
# 需要在 GitHub Secrets 中設定 | Required in GitHub Secrets:
# - UPSTASH_REDIS_REST_URL
# - UPSTASH_REDIS_REST_TOKEN
# - CLOUDFLARE_R2_ACCESS_KEY_ID
# - CLOUDFLARE_R2_SECRET_ACCESS_KEY
# - CLOUDFLARE_R2_BUCKET_NAME

# ===========================================
# 🛡️  安全性檢查清單 | Security Checklist
# ===========================================
# □ .env.local 未被提交到 Git
# □ 生產環境使用不同的資料庫和 API 金鑰
# □ Supabase RLS (Row Level Security) 已啟用
# □ API 速率限制已設定
# □ 定期輪換 API 金鑰
