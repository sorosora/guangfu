# 本地開發環境變數範本 | Local Development Environment Variables Template
# 複製此檔案為 .env.local 並填入真實的開發用 API 金鑰
# Copy this file to .env.local and fill in real development API keys

# ===========================================
# 🔧 本地開發設定 | Local Development Setup
# ===========================================

# Supabase 本地開發配置 | Supabase Local Development Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-dev-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ... # 開發環境的匿名金鑰 | Dev environment anon key
SUPABASE_SERVICE_ROLE_KEY=eyJ... # 開發環境的服務角色金鑰 | Dev environment service role key

# Upstash Redis 開發配置 | Upstash Redis Development Configuration
UPSTASH_REDIS_REST_URL=https://your-dev-redis.upstash.io
UPSTASH_REDIS_REST_TOKEN=AYA... # 開發環境 Redis 令牌 | Dev environment Redis token

# Cloudflare R2 開發配置 | Cloudflare R2 Development Configuration
CLOUDFLARE_R2_ACCESS_KEY_ID=your_dev_access_key
CLOUDFLARE_R2_SECRET_ACCESS_KEY=your_dev_secret_key
CLOUDFLARE_R2_BUCKET_NAME=guangfu-dev # 開發環境儲存桶 | Development bucket

# 圖磚 CDN 開發 URL | Tiles CDN Development URL
NEXT_PUBLIC_TILES_BASE_URL=https://dev-tiles.yourdomain.com

# KMZ 檔案基礎 URL | KMZ Files Base URL
NEXT_PUBLIC_KMZ_BASE_URL=https://your-r2-endpoint.r2.cloudflarestorage.com

# 測試區域配置 | Test Area Configuration
# 🎯 設定您的實際測試位置座標 | Set your actual test location coordinates
# 這些座標會覆蓋程式碼中的預設測試區域 | These coordinates will override the default test area in code

# 範例：台北市中心區域 | Example: Central Taipei Area
NEXT_PUBLIC_TEST_AREA_NORTHWEST_LAT=25.05500
NEXT_PUBLIC_TEST_AREA_NORTHWEST_LON=121.51000
NEXT_PUBLIC_TEST_AREA_NORTHEAST_LAT=25.05500
NEXT_PUBLIC_TEST_AREA_NORTHEAST_LON=121.55000
NEXT_PUBLIC_TEST_AREA_SOUTHWEST_LAT=25.02000
NEXT_PUBLIC_TEST_AREA_SOUTHWEST_LON=121.51000
NEXT_PUBLIC_TEST_AREA_SOUTHEAST_LAT=25.02000
NEXT_PUBLIC_TEST_AREA_SOUTHEAST_LON=121.55000

# 測試區域中心點 | Test Area Center Point
NEXT_PUBLIC_TEST_AREA_CENTER_LAT=25.03750
NEXT_PUBLIC_TEST_AREA_CENTER_LON=121.53000

# ===========================================
# 💡 本地開發建議 | Local Development Tips
# ===========================================

# 1. 使用開發專用的 Supabase 專案
#    建議建立與生產環境分離的開發專案，避免影響正式資料

# 2. 使用開發專用的 Redis 資料庫
#    Upstash 提供免費的開發用 Redis 資料庫

# 3. 使用開發專用的 R2 儲存桶
#    建立獨立的開發儲存桶，避免與生產環境混淆

# 4. 測試區域配置
#    設定您當前位置附近的測試區域座標
#    建議使用您實際所在位置周圍 4km x 3km 的範圍
#    可以使用 Google Maps 或其他地圖工具獲取精確座標
#
#    測試區域設定步驟：
#    a. 確定您要測試的區域中心點
#    b. 計算四個角落的座標（北西、北東、南西、南東）
#    c. 在 .env.local 中設定所有測試區域環境變數
#    d. 啟動開發服務器，切換到測試模式進行驗證

# ===========================================
# 🛠️  快速設定指令 | Quick Setup Commands
# ===========================================

# 1. 複製環境變數檔案
#    cp .env.local.example .env.local

# 2. 安裝依賴並啟動開發伺服器
#    yarn && yarn dev

# 3. 檢查環境變數是否正確載入
#    yarn dev 時會在控制台顯示 Supabase 連線狀態

# ===========================================
# ⚠️  開發注意事項 | Development Notes
# ===========================================

# • 請勿在 .env.local 中使用生產環境的 API 金鑰
# • 定期檢查 .env.local 是否被 .gitignore 正確忽略
# • 如果遇到權限問題，請檢查 Supabase RLS 政策設定
# • Redis 連線問題通常是網路或金鑰錯誤造成
# • R2 上傳失敗請檢查 CORS 設定和權限配置
